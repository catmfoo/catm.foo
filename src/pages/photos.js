import * as React from "react";
import Layout from "../components/layout";

export default function Photos({ location }) {
  return (
    <Layout location={location}>
      <div>this is the photos page</div>
    </Layout>
  );
}
