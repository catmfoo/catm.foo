import * as React from "react";
import Layout from "../components/layout";

export default function FourOFour({ location }) {
  return (
    <Layout location={location}>
      <div>404 - sorry, that's not a real page :(</div>
    </Layout>
  );
}
